# valid_parentheses

Implement `is_valid(s)` in `solution.py`.

`s` contains only the characters `()[]{}`. Return `True` if every opening
bracket is closed by the same type of bracket in the correct order, and every
closing bracket matches the most recent unclosed opening bracket. An empty
string is valid.
