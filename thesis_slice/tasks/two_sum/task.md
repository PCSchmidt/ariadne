# two_sum

Implement `two_sum(nums, target)` in `solution.py`.

Given a list of integers `nums` and an integer `target`, return the two indices
`[i, j]` with `i < j` such that `nums[i] + nums[j] == target`. Exactly one valid
answer exists. Return the indices as a list in ascending order.
